<%= stylesheet_link_tag 'events' %>

<div class="container">
  <div class="table-responsive" id="events">
    <%= render partial: "events_search" %>
  </div>
  <div id="add_event">
    <%= link_to 'Créer un évènement', new_event_path, class: 'btn btn-default btn-lg' if member_signed_in? %>
  </div>
</div>
